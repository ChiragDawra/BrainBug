{
    "model_name": "Salesforce/codet5-base",
    "output_dir": "ml_models/codet5-error-analyzer",
    "num_epochs": 10,
    "batch_size": 1,
    "eval_batch_size": 1,
    "gradient_accumulation_steps": 16,
    "learning_rate": 3e-5,
    "warmup_steps": 500,
    "save_steps": 500,
    "eval_steps": 500,
    "max_length": 384,
    "logging_dir": "logs/error-analyzer",
    
    "_comments": {
        "optimization_summary": "Ultra-safe config for RTX 4050 6GB VRAM with FP16 fix",
        "changes_made": [
            "Reduced batch_size to 1 for maximum safety",
            "Increased gradient_accumulation to 16 to maintain effective batch size",
            "FP16 compatibility fixes in training script",
            "Added proper gradient clipping and optimizer settings"
        ],
        "memory_usage": "~3-4GB VRAM (very safe for 6GB GPU)",
        "effective_batch_size": "1 * 16 = 16",
        "training_speed": "Slower but stable"
    }
}