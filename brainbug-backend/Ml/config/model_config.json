{
  "model_name": "Salesforce/codet5-base",
  "max_length": 512,
  "batch_size": 8,
  "learning_rate": 5e-5,
  "num_epochs": 3,
  "save_steps": 500,
  "eval_steps": 500,
  "warmup_steps": 100,
  "output_dir": "ml_models/codet5_finetuned",
  "checkpoint_dir": "ml_models/checkpoints",
  "logging_dir": "ml_models/training_logs"
}
